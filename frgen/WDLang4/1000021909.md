


## HEnregistrementVersJSON (Fonction)

***En anglais : HRecordToJSON***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Récupère la structure et la valeur de l'enregistrement en cours et les exporte dans une chaîne de caractères au format JSON.

**Remarque**: Cette fonction est utilisable aussi bien sur les tables que sur les requêtes.




<a name="Exemple1"></a>
<a name="sample_code"></a>

## Exemple


```wl
// Exporte l'enregistrement en cours de la dernière table utilisée
sJSON est une chaîne = HEnregistrementVersJSON()
```
<a name="Exemple2"></a>

```wl
// Exporte l'enregistrement en cours de la table "Client"
sJSON est une chaîne 
sJSON = HEnregistrementVersJSON(Client)
```
<a name="Exemple3"></a>

```wl
// Exporte l'enregistrement en cours de la table "Client"
// Remarque: seules les rubriques NOM et NUMERO sont prises en compte
sJSON est une chaîne 
sJSON = HEnregistrementVersJSON(Client, "NOM,NUMERO")
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntaxe

`<Résultat> = HEnregistrementVersJSON([<Table> [, <Liste des rubriques>]])`
---

**`<Résultat> : (Chaîne de caractères)`**

Structure et valeur de l'enregistrement en cours au format JSON. Cette structure est représentée sous la forme:


```txt
{"Fichier":
	{
		"Rubrique1":Donnée de la rubrique 1,
		"Rubrique2":Donnée de la rubrique 2,
		...
		}
}
```


**`<Table> : (Chaîne de caractères optionnelle)`**

Nom de la table HFSQL manipulée ou de la requête manipulée. Si ce paramètre n'est pas précisé, la dernière table HFSQL utilisée sera prise en compte.

**`<Liste des rubriques> : (Chaîne de caractères optionnelle)`**

Rubriques à exporter. 
Si plusieurs rubriques sont spécifiées, le nom des rubriques doit être séparé par une virgule. 
Les rubriques de type clé composée, mémo binaire ou binaire sont ignorées. 
Si ce paramètre n'est pas précisé ou correspond à une chaîne vide (""), toutes les rubriques sont exportées (sauf les rubriques de type clé composée, mémo binaire ou binaire) dans l'ordre physique de description.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarques


### Rubriques tableau
<a name="rubriques_tableau_ELTPARAGRAPHE000096"></a>Si une des rubriques exportées est une rubrique de type tableau, chaque élément de la rubrique tableau est exporté dans un nouveau niveau:


```txt
{"Fichier":
	{
	"Rubrique1":Donnée de la rubrique 1,
	"Rubrique2":Donnée de la rubrique 2,
	"RubriqueTab":
		[Donnée élément 1, Donnée élément 2, ...]
	...
	}
}
```

**Rappel**: Les rubriques de type clé composée, mémo binaire ou binaire sont ignorées.

<a name="XComposante"></a>

## Classification Métier / UI:
Code métier
## Composante:
wd310hf.dll
## Voir aussi

[Fonctions de gestion HFSQL](../WDLang4/3044156.md)

[HExporteJSON (Fonction)](../WDLang4/1000021964.md)

[HImporteJSON (Fonction)](../WDLang4/1000021965.md)

[&lt;Source&gt;.EnregistrementVersJSON (Fonction)](../WDLang4/1000024607.md)

[HJSONVersEnregistrement (Fonction)](../WDLang4/1000021910.md)

[Fonctions de manipulation des variables JSON](../WDLang4/1410091296.md)

