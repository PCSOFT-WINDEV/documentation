


## Mettre en place la réplication journalée : Modifier le schéma des données
			



<a name="NOTE1"></a>
<a name="NOTE1_1"></a>


## Présentation
<a name="presentation_ELTTEXTE000187"></a>
Il est possible de mettre en place la réplication journalée dans l'éditeur de schémas des données :

- soit directement lors de la création d'une table. L'assistant de création de la table demande si la réplication journalée doit être gérée pour cette table.

- soit sur une table existante (que l'application ait été diffusée ou non).




<a name="NOTE2"></a>
<a name="NOTE2_1"></a>


## Gérer la réplication journalée pour une table
<a name="gerer_replication_journalee_pour_une_table_ELTTEXTE000211"></a>
Pour mettre en place la réplication journalée sur une table :

1. Ouvrez votre schéma des données sous l'éditeur de schémas des données.

2. Pour toutes les tables en réplication, cochez l'option "Réplication journalée (pour compatibilité avec les anciennes versions)" (onglet "Divers" de la description de la table). Si vos tables ne respectent pas les conditions d'utilisation de la réplication (voir paragraphe suivant), différents messages sont affichés.
	**Attention** : lors de la mise en place de la réplication journalée, la journalisation est automatiquement mise en place. En effet, la journalisation permet de connaître simplement toutes les opérations réalisées sur les tables afin de les reporter sur les différentes bases de données. Pour plus de détails sur la journalisation, consultez [La journalisation](../WDLang4/3044184.md).

3. Générez votre schéma des données.




<a name="NOTE3"></a>
<a name="NOTE3_1"></a>


## Conséquences de la mise en place de la réplication journalée
<a name="consequences_mise_place_replication_journalee_ELTTEXTE000235"></a>


### Conditions nécessaires ou conseillées
<a name="conditions_necessaires_conseillees_ELTPARAGRAPHE000041"></a>

Lors de la mise en place de la réplication journalée sur une table, WINDEV et WEBDEV :

- **Proposent d'utiliser un identifiant automatique sur 8 octets dans les tables en réplication journalée** : 
	En effet, pour simplifier la réplication journalée, il est conseillé d'utiliser un identifiant automatique dans vos tables. Cet identifiant automatique est entièrement géré par WINDEV et WEBDEV. Pour éviter les erreurs de doublons lors de la réplication, des intervalles de numéros d'identifiant sont attribués à chaque site participant à la réplication. 
	L'utilisation d'un identifiant automatique est fortement conseillé mais non obligatoire. En effet, il est toujours possible de gérer un identifiant personnalisé, composé par exemple d'un identifiant automatique, et des initiales du site ou de la personne créant l'enregistrement.

- **Mettent en place la journalisation pour la table sélectionnée** : 
	En effet, la journalisation permet de connaître simplement toutes les opérations réalisées sur les tables afin de les reporter sur les différentes bases de données. La journalisation de type "Journal des écritures + historique des accès" est mise automatiquement en place lorsque l'option "Réplication journalée" est cochée.



<a name="NOTE3_2"></a>


### Répertoires de création des fichiers nécessaires à la réplication journalée
<a name="repertoires_creation_des_fichiers_necessaires_replication_journalee_ELTPARAGRAPHE000057"></a>

La réplication journalée est étroitement liée à la journalisation. Lors de la mise en place de la réplication journalée dans une application, la journalisation est automatiquement activée.

Pour toutes les tables manipulées (tables, fichiers journal ou fichiers de la réplication), il est possible de définir les différents chemins des fichiers physiques correspondant aussi bien sous l'éditeur de schémas des données que par programmation.

Par défaut, l'arborescence des tables est la suivante (exemple pour l'application GesCom) :<br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=213&name=Replic_repertoire.gif)
Le tableau ci-dessous présente la valeur par défaut de ces différents répertoires et leur mode de configuration aussi bien sous l'éditeur de schémas des données que par programmation.

|   | Valeur par défaut | Où le configurer sous l'éditeur de schémas des données ? | Comment changer la valeur par défaut par programmation ? |
| --- | --- | --- | --- |
| **Répertoire des tables** | Répertoire de l'exécutable. | <br><br>- Pour toutes les tables de l'application : onglet "Détail" de la description du schéma des données<br><br>- Pour chaque table : onglet "Info" de la description de la table.<br><br><br> | Pour modifier le répertoire par défaut des tables de l'application, utilisez la fonction [HSubstRep](../WDLang4/3044028.md).<br>Lors de la création des fichiers de la journalisation et de la réplication, les répertoires créés seront relatifs à ce nouveau répertoire. <br>Remarque : Pour modifier uniquement le répertoire d'une des tables, utilisez la fonction [HChangeRep](../WDLang4/3044168.md). |
| **Répertoire des fichiers de la réplication** | Sous-répertoire RPL du répertoire par défaut des tables. | Onglet "Journal\\Réplica" de la description du schéma des données. | Précisez le répertoire voulu dans les fonctions [HCréeRéplicaMaître](../WDLang4/3044175.md) et [HCréeRéplicaAbonné](../WDLang4/3044253.md). |
| **Répertoire des fichiers de la journalisation** | Sous-répertoire JNL du répertoire par défaut des tables. | <br><br>- Pour toutes les tables de l'application : onglet "Journal\\Réplica" de la description du schéma des données.<br><br>- Pour chaque table : onglet "Divers" de la description de la table.<br><br><br> | Pour modifier le répertoire des fichiers journaux, utilisez la fonction [HChangeRepJNL](../WDLang4/3044164.md). Cette fonction permet :<br><br>- soit de changer le répertoire du fichier Journal (Fichier JNL)<br><br>- soit de changer le répertoire du fichier JNL et des fichiers de journalisation (fichiers JournalIdentification et JournalOperation).<br><br><br> |


**Attention** : Pour gérer la réplication journalée dans une application, la table des opérations du journal (table JournalOperation.fic) doit être identique pour toutes les tables en réplication dans un même schéma des données.

<a name="NOTE4"></a>
<a name="NOTE4_1"></a>


## Génération du schéma des données
<a name="generation_schema_des_donnees_ELTTEXTE000265"></a>
Pour prendre en compte la mise en place de la réplication journalée dans le schéma des données, **il est nécessaire de générer le schéma des données.**

Si l'application manipule déjà des tables, cette génération du schéma des données entraîne une modification automatique des tables pour :

- prendre en compte la réplication journalée,

- prendre en compte la journalisation (si nécessaire)

- mettre en place les identifiants automatiques.




Cette modification des tables devra également être effectuée lors de l'installation de l'application (si l'application a déjà été déployée).

**Attention** : Si des journaux existaient déjà, ces journaux sont automatiquement purgés lors de la modification automatique. Avant de lancer la modification automatique, il est possible de sauvegarder les journaux existant grâce à l'administrateur de journaux WDLog.


## Voir aussi

[Réplication journalée](../WDLang4/3044256.md)

