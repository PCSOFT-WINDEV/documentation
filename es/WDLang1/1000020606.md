


## &lt;Menú&gt;.InsertarMenú (Función)



<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Inserta un menú antes que otro menú en una ventana. El menú insertado puede contener otros menús, opciones de menú, separadores, ...

Para añadir un menú al final de un menú existente, utilice [&lt;Menú&gt;.AddMenu](../WDLang1/1000020592.md).


<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
// Insert a new menu
_Menu.InsertMenu("MNU_Schedule", "MNU_Monitoring", "Project monitoring")
// Add an option into the new menu
MNU_Monitoring.AddOption("MNU_Prj", "Sales management", Sales_Mgt)
```

<a name="XSYNTAX"></a>
<a name="SYNTAX1"></a>

## Sintaxis

### Insertar un menú (posición definida por un nombre de menú)

`<Result> = <Source menu>.InsertMenu(<Insert menu> , <Menu to create> , <Caption>)`
---

**`<Result>: (Integro)`**



- Posición del menú en el sub-menú, 

- -1 si se produce un error.




**`<Source menu>: (El nombre del menú)`**

Nombre del menú al que se añadirá el nuevo menú. El nuevo menú se añadirá en la posición especificada.

**`<Insert menu>: (El nombre del menú)`**

Nombre del menú utilizado como posición base para la inserción. El nuevo menú se insertará antes de esta posición.

**`<Menu to create>: (Cadena de caracteres)`**

Nombre del menú para crear e insertar. Este nombre se utilizará para manipular el menú mediante programación. Si este nombre corresponde a un menú que ya existe, se produce un error fatal.

**`<Caption>: (Cadena de caracteres)`**

Texto del nuevo menú. Esta leyenda se mostrará en la ventana.


<a name="SYNTAX2"></a>

### Insertar un menú (posición definida por un menú índice)

`<Result> = <Source menu>.InsertMenu(<Insertion index> , <Menu to create> , <Caption>)`
---

**`<Result>: (Integro)`**



- Posición del menú en el sub-menú, 

- -1 si se produce un error.




**`<Source menu>: (El nombre del menú)`**

Nombre del menú al que se añadirá el nuevo menú. El nuevo menú se añadirá en la posición especificada.

**`<Insertion index>: (Integro)`**

Subíndice del menú utilizado como posición base para la inserción. El nuevo menú se insertará antes de esta posición.

**`<Menu to create>: (Cadena de caracteres)`**

Nombre del menú para crear e insertar. Este nombre se utilizará para manipular el menú mediante programación. Si este nombre corresponde a un menú que ya existe, se produce un error fatal.

**`<Caption>: (Cadena de caracteres)`**

Texto del nuevo menú. Esta leyenda se mostrará en la ventana.



<a name="NOTE0"></a>
<a name="XComponent"></a>

## Componente:
wd280obj.dll
