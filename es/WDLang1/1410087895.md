


## <Camera>.Photo (Función)



<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Captura una foto a través de un control Cámara en una aplicación Android o iOS. La foto se guarda en el directorio predeterminado de la aplicación, o en el directorio especificado con la propiedad [WorkingDirectory](../Proprietes/1000026169.md). Las diferentes opciones especificadas para el control Cámara (flash, cámara trasera o delantera, etc.) se tienen en cuenta. 

Observación: Esta función solo puede utilizarse con un control Cámara de la versión 27 o posterior (la opción "Modo compatible con la versión 26" no debe estar marcada en la pestaña "General" de la ventana de descripción del control). 
<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
CAM_MyCamera.Photo(CameraPhoto_Callback)

INTERNAL PROCEDURE CameraPhoto_Callback(bSuccess is boolean, PhotoPath is string)
	IF bSuccess = True THEN
		IMG_PHOTO = PhotoPath
		STC_PHOTO_PATH = PhotoPath
		MyWindow.Plane = 3
	ELSE
		ToastDisplay("Cannot take photo." + CR + ErrorInfo())
	END
END
```

<a name="XSYNTAX"></a>

## Sintaxis
<a name="SYNTAX1"></a>

`<Camera control>.Photo(<WLanguage procedure>)`
---

**`<Camera control>: (Nombre del control)`**

Nombre del control Cámara que capturará la foto.

**`<WLanguage procedure>: (Nombre de Procedure)`**

Nombre del procedimiento WLanguage ("callback") al que se llama cuando se guarda la foto.
Para obtener más información sobre este procedimiento, consulte [Parámetros del procedimiento utilizado por la función <Camera>.Photo](../WDLang1/1410087851.md).



<a name="XComponent"></a>

## Componente:
wd280android.aar
