


## <Chart>.AddData (Función)



<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Agrega un dato a un gráfico. 

**Atención**: Para los gráficos "avanzados" hay que utilizar una función específica de WLanguage (véase "Observaciones"). 

![Linux](https://doc.pcsoft.fr/ext/images/us/LX.png) **Atención**: Se requiere una configuración específica para usar esta función en Linux. Para obtener más información, consulte [Los gráficos](../WDChamp/3042060.md). 










<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
// Add a data into the "CHART_MyChart" control. 
// This data (whose value is 10) is added at the end of the first series.
CHART_MyChart.AddData(1, 10)
```

<a name="XSYNTAX"></a>
<a name="SYNTAX1"></a>

## Sintaxis

`<Chart control>.AddData(<Series number> [, <Index>] , <Value>)`
---

**`<Chart control>: (Nombre del control)`**

Nombre de la control Gráfico a utilizar (en la ventana, página o editor de reportes).


**`<Series number>: (Integro)`**

Número de la serie a la que se añadirá un valor. 
Por ejemplo, si la control Gráfico muestra 2 Line s, esto se usa para identificar la Line para la cual los datos deben ser añadidos. 

Si el número especificado no existe, las series entre la última serie existente y el número especificado se crean automáticamente y se inicializan a 0.

**`<Index>: (Entero opcional)`**

Índice de los datos de la serie a los que hay que añadir un valor. 
Por ejemplo, para un gráfico de Line, este parámetro corresponde al eje X del valor a añadir. 

Si este parámetro:

- no se especifica, el valor se añade al final de la serie.

- se especifica, se sustituye el valor anterior.




**`<Value>: (Entero o real)`**

Valor añadido. 

Por ejemplo, para un gráfico de Line, este parámetro corresponde al eje Y del valor a añadir.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Observaciones


### Añadir datos
<a name="anadir_datos_ELTPARAGRAPHE000277"></a>

- Los datos agregados con la función **<Chart>.AddData** se tendrán en cuenta la próxima vez que se llame a la función [&lt;Dibujo](../WDLang3/1000024062.md).

- Para un gráfico circular (constante *grPie*), solo se utilizan los datos de la primera serie. No se puede dibujar un gráfico circular si los datos encontrados en la primera serie son nulos.

- Para los gráficos de "barras apiladas", si el valor de la primera serie es negativo, la barra correspondiente se orienta hacia abajo..



<a name="NOTE0_2"></a>


### Añadir datos a los diferentes tipos de gráficos
<a name="anadir_datos_los_diferentes_tipos_graficos_ELTPARAGRAPHE000296"></a>

- Para los gráficos de dispersión, se recomienda utilizar [.ScatterAddDataXY](../WDLang3/1000024105.md).

- Para los gráficos de acciones, se recomienda utilizar [.StockAddData](../WDLang3/1000023565.md).

- Para los gráficos de mapas de calor, se recomienda utilizar [&lt;Gráfico&gt;.HMAddData](../WDLang3/1000024079.md).

- Para las cartas Sunburst, se recomienda utilizar [&lt;Gráfico&gt;.SunburstAddData](../WDLang3/1000024132.md). 

- Para los gráficos de superficie, se recomienda utilizar [&lt;Tabla&gt;.SurfaceAddData](../WDLang3/1000024143.md).

- Para los gráficos de barras radiales, se recomienda utilizar [.RBAddData](../WDLang3/1410088092.md).

- Para añadir un valor a los datos de un gráfico, utilice [.IncreaseData](../WDLang3/1000024086.md).





<a name="XComponent"></a>

## Clasificación Lógica de negocio / UI:
Código neutro
## Componente:
wd280grf.dll
