


## <Chart>.CategoryLabel (Función)



<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Permite: 

- Inicializar la etiqueta de una categoría de datos (o sección) en un gráfico. 
	

- get la etiqueta de una categoría de datos (o sección) en un gráfico.




Ejemplo de etiqueta de categoría:<br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=7&name=EtiquetteCat.gif)


**Recordatorio**: Un elemento de la carta no tiene la etiqueta de Default.

![Linux](https://doc.pcsoft.fr/ext/images/us/LX.png) **Atención**: Se requiere una configuración específica para usar esta función en Linux. Para obtener más información, consulte [Los gráficos](../WDChamp/3042060.md). 


<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
// Initialize the "February" label of the second data category
// in "MyChart"
CHART_MyChart.CategoryLabel(2, "February")
```
<a name="3042015_Example3"></a>

```wl
// Read label of second data category
// in "MyChart"
MyLabel is string = CHART_MyChart.CategoryLabel(2)
```

<a name="XSYNTAX"></a>
<a name="SYNTAX1"></a>

## Sintaxis

### Inicialización de la etiqueta de la categoría (o sección) de datos en un gráfico

`<Chart control>.CategoryLabel(<Subscript> , <Label>)`
---

**`<Chart control>: (Nombre del control)`**

Nombre de la control Gráfico a utilizar (en la ventana, página o editor de reportes).


**`<Subscript>: (Integro)`**

Índice de datos de la serie.

**`<Label>: (Cadena de caracteres)`**

Leyenda de la etiqueta a inicializar. 
Este parámetro también puede corresponder a una duración, una fecha, una hora o una fecha-hora.. 
**Observación**: Si se utiliza una escala de abscisas temporal, se debe llamar sistemáticamente a **<Chart>.CategoryLabel** cada vez que se añadan datos al gráfico con [.AddData](../WDLang3/1000023533.md).

<a name="SYNTAX2"></a>

### Averiguar la etiqueta de la categoría (o sección) de datos en un gráfico

`<Result> = grCategoryLabel(<Chart control> , <Subscript>)`
---

**`<Result>: (Cadena de caracteres)`**

Leyenda de la etiqueta.

**`<Chart control>: (Nombre del control)`**

Nombre de la control Gráfico a utilizar (en la ventana, página o editor de reportes).


**`<Subscript>: (Integro)`**

Índice de datos (con la etiqueta deseada) en la serie.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Observaciones


### Gestión de las etiquetas de las categorías
<a name="gestion_las_etiquetas_las_categorias_ELTPARAGRAPHE000341"></a>

Se visualizan las etiquetas de categoría:

- por debajo de las marcas horizontales para los gráficos de Line y de acciones (Candlestick y BarCharts),

- debajo del eje X para los gráficos de Columna y MinMax.

<br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=7&name=EtiquetteCat.gif)
Las etiquetas de categoría no pueden utilizarse en los gráficos de dispersión.

La posición de las etiquetas de las categorías no se puede cambiar. 

Las etiquetas de categorías se definen con la función **<Chart>.CategoryLabel**.

Los datos definidos con la función **<Chart>.CategoryLabel** se tendrán en cuenta la próxima vez que se llame a la función [&lt;Dibujo](../WDLang3/1000024062.md).

El tipo de letra de la etiqueta puede ser modificado por [&lt;Tabla&gt;.LabelFont](../WDLang3/1000024117.md).

Las etiquetas de categorías definidas con la función **<Chart>.CategoryLabel** se pueden mostrar en las etiquetas de datos del gráfico circular (constante *grShowLabel* utilizada con la función [&lt;Tabla&gt;.etiqueta](../WDLang3/1000024165.md)).



<a name="XComponent"></a>

## Componente:
wd280grf.dll
