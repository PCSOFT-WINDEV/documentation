
## <Source>.ToPage (Función)

<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Actualiza automáticamente los controles de una Page con: 

- los valores de los elementos vinculados en el Record actual (cargado en memoria) del archivo de datos HFSL descrito en el editor de análisis. 

- los valores de las variables vinculadas de la lengua WL. 


Esta operación se realiza independientemente del estado de los mandos (grises, inactivos o Invisible).

Observaciones: 

- **<Source>.ToPage** no lee registros en el archivo de datos. El valor de la Record cargado en memoria se utiliza para actualizar los controles. Por lo tanto, un Record debe ser cargado en memoria previamente (con [&lt;Fuente&gt;.Read](../WDLang4/1000025019.md), [&lt;Fuente&gt;.ReadSeek](../WDLang4/1000025023.md), [&lt;Fuente&gt;.ReadFirst](../WDLang4/1000025022.md), etc.).



<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
HReadSeekFirst(CUSTOMER, NAME, "PAUL")
IF HFound() = True THEN CUSTOMER.ToPage()

// Read the record and 
// update the controls with the values from the items
HReadFirst(CUSTOMER, NAME)
CUSTOMER.ToPage()
```

<a name="XSYNTAX"></a>
<a name="SYNTAX1"></a>

## Sintaxis

`<Source>.ToPage([<Page>])`
---

**`<Source>: (Tipo de fuente especificada)`**

Nombre de la fuente a la que están vinculados los controles que se van a actualizar. Esta fuente puede ser:

- El nombre de un archivo de datos: sólo se tienen en cuenta los controles vinculados a los elementos del archivo de datos.

- El nombre de un WLanguage Variable: sólo se tienen en cuenta los controles vinculados a la lengua de trabajo Variable. El control se vinculará a los datos Variable (por ejemplo, un Variable que contenga el nombre de otro Variable).  




**`<Page>: (Nombre opcional Page)`**

Nombre de la página donde se deben actualizar los controles con los valores de la Record actual en el archivo de datos.

Si este parámetro no se especifica o es una cadena vacía (""), se utiliza la Page que contiene el código actual.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Observaciones


### Vinculación entre un control y un WLanguage Variable o entre un control y un elemento de un archivo de datos
<a name="vinculacion_entre_control_wlanguage_variable_entre_control_elemento_archivo_datos_ELTPARAGRAPHE000232"></a>

Se puede determinar la vinculación entre un control y un WLanguage Variable o entre un control y un elemento de un archivo de datos:

- En el editor, en la ventana de control description (pestaña "Binding").

- De forma programada, con el programa [DataBinding](../Proprietes/2510060.md) Property.



<a name="NOTE0_2"></a>


### Vinculación a un elemento: tipos de fijaciones
<a name="vinculacion_elemento_tipos_fijaciones_ELTPARAGRAPHE000248"></a>

Existen dos tipos de vínculos entre una control y un artículo

- *unión directa*: En este caso, **<Source>.ToPage** transfiere el valor de los elementos del Record cargado en la memoria al control enlazado f006c00 , sin leer ningún valor.

- *enlace leyendo los registros en el archivo de datos associated* .



<a name="NOTE0_3"></a>


### Varios
<a name="varios_ELTPARAGRAPHE000263"></a>

- Los caracteres de espacio en los elementos HFSQL se mantienen.

- **<Source>.ToPage** actualiza los controles Image vinculados a elementos de memo Image .

- **<Source>.ToPage** no está disponible en vistas. Los valores de los elementos deben asignarse a cada control.

- **Vinculación con elementos de "Contraseña segura"**:

	- En una Edición control vinculada a un elemento Contraseña, aparecen los caracteres "\*\*\*\*\*".. Si la Edición control es de tipo "Contraseña", se muestran puntos en su lugar. 

	- El valor del elemento "Contraseña" no puede volver a leerse hasta que el usuario cambie el valor de la contraseña.

	- El valor del elemento "Contraseña" puede seguir comparándose con una cadena (por ejemplo, "" o "1234").







<a name="XComponent"></a>

## Componente:
wd280.dll
