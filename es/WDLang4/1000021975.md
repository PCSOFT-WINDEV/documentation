
## HClusterSynchronizationErrorInfo( Función)



<a name="XUse"></a>
<a name="Use"></a>
<a name="description"></a>
Devuelve el error que se produjo durante el paso de sincronización anterior del clúster HFSQL.

Recordatorio: Una sincronización se realiza automáticamente cuando un nodo de cluster se detiene anormalmente, por ejemplo: cuando se reinicia, este nodo se sincroniza con otro nodo que contiene los datos actualizados.
<a name="Example1"></a>
<a name="sample_code"></a>

## Ejemplo


```wl
sSyncError is string
sSyncError = HClusterSynchronizationErrorInfo("MyCoordinator", "MyPassword", "NodeName")
```

<a name="XSYNTAX"></a>

## Sintaxis
<a name="SYNTAX1"></a>

`<Result> = HClusterSynchronizationErrorInfo(<Coordinator> , <Password> , <Node>)`
---

**`<Result>: (Cadena de caracteres)`**

Error ocurrido durante la última sincronización.

**`<Coordinator>: (Cadena de caracteres)`**

Nombre DNS del ordenador donde está instalado el coordinador del clúster.

**`<Password>: (Cadena de caracteres)`**

Contraseña utilizada para conectarse con el coordinador. Esta contraseña se dio cuando [instalación del cluster](../WDLang4/1000019031.md).

**`<Node>: (Cadena de caracteres)`**

Nombre DNS del nodo a utilizar. 







<a name="XComponent"></a>

## Clasificación Lógica de negocio / UI:
Lógica de negocio
## Componente:
wd280hf.dll
