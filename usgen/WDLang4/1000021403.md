
## &lt;Source&gt;.FromWindow (Function)

***In french: &lt;Source&gt;.DepuisFenêtre***

<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Automatically initializes:

- the values of a table's items in memory with the values from the controls in the window. The table items bound to the window controls are automatically updated with the values of these controls.

- the value of the WLanguage variables with the value of the window controls. The WLanguage variables bound to the window controls are automatically updated with the values of these controls.




**Remarks**:

- The function **&lt;Source&gt;.FromWindow FUNCTION** function does not write to the table: only the variables of the table fields are assigned (i.e., the variables of the record loaded into memory). To modify the records, use [&lt;Source&gt;.Add](../WDLang4/1000024187.md), [&lt;Source&gt;.Modify](../WDLang4/1000025038.md), [&lt;Source&gt;.Write](../WDLang4/1000024331.md), etc.

- ![WEBDEV - Server code](https://doc.pcsoft.fr/ext/images/us/WBS.png)![PHP](https://doc.pcsoft.fr/ext/images/us/PHP.png) This function is also available for WEBDEV pages. It is equivalent to [&lt;Page&gt;.ToSource](../WDLang4/1000021392.md). 

- ![PHP](https://doc.pcsoft.fr/ext/images/us/PHP.png) Only the binding to the table items is supported. Bindings to WLanguage variables are not supported in this version.



<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Click the "Add" button
Customer.FromWindow()
Customer.Add()
IF HErrorDuplicates() = True THEN Error("This number already exists")
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Source>.FromWindow([<Window>])`
---

**`<Source>: (Type corresponding to the specified source)`**

Name of the source containing the data to update. For example, this source can be:

- The name of a table. In this case, only the controls bound to the items of the table are taken into account.

- The name of a WLanguage variable. In this case, only the controls bound to the WLanguage variable are taken into account. The control will be bound to the variable data (e.g., a variable containing the name of another variable). 




**`<Window>: (Window name)`**

Name of the window that contains the controls.

Summary: Called when a control gains focus. 



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Binding between a control and a WLanguage variable or between a control and a table item
<a name="binding_between_control_and_wlanguage_variable_between_control_and_table_item_ELTPARAGRAPHE000235"></a>

The binding between a control and a WLanguage variable or between a control and a table item can be determined:

- In the editor, in the control description window ("Binding" tab).

- Programmatically, with the [DataBinding](../Proprietes/2510060.md) property.



<a name="NOTE0_2"></a>


### Section updates: Special cases
<a name="section_updates_special_cases_ELTPARAGRAPHE000251"></a>



- To automatically assign the NULL value to an item with **&lt;Source&gt;.FromWindow**:

	1. Check "Support NULL values" in the table description. 

	2. Check "Allow NULL values" for the item in the data model editor.

	3. Check "Return NULL if empty" in the "Details" tab of the Edit control bound to the item.




- The items of the table that are not bound to a control are not updated by **&lt;Source&gt;.FromWindow**.

- If multiple controls in the window or page are bound to the same table item, **&lt;Source&gt;.FromWindow** may not update the bound item properly. ***It is not recommended to bind multiple controls of a window or page to the same item***.

- **&lt;Source&gt;.FromWindow** does not update the items with the values from Image controls. Use the [&lt;Source&gt;.LinkMemo](../WDLang4/1000024194.md) function.

- **&lt;Source&gt;.FromWindow** is not available on views.

- **Link with "Secure password" fields** The value of the "Secure password" field is not modified until the user modifies the value in the field linked to the field.




<a name="XComposante"></a>

## Component:
wd310obj.dll
## Voir aussi

[ControlError (Function)](../WDLang4/3025036.md)

[&lt;Window&gt;.FromFile (Function)](../WDLang4/1000021387.md)

[&lt;Page&gt;.ToFile (Function)](../WDLang4/1000021390.md)

[NULL values in HFSQL](../WDLang4/3044187.md)

[HFSQL functions (prefix syntax)](../WDLang4/1000025085.md)

[&lt;Window&gt;.ToSource (Function)](../WDLang4/1000021386.md)

[SourceFromWindow (Function)](../WDLang4/1000021402.md)

