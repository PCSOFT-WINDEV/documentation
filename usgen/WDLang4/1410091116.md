
## Parameters of the procedure used by HStatCalculateAsynchronous



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
WLanguage procedure ("callback") called by [HStatCalculateAsynchronous](../WDLang4/1410091108.md) for each HFSQL Client/Server table checked.


<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
DatabaseConnection is Connection
FileArray is array of hFileParameter

MyFileParam is hFileParameter
MyFileParam.Name = "AvailableAction"
// MyFileParam.Password = ""
// The Option property is ignored
ArrayAdd(FileArray, MyFileParam)

IF bBackgroundTask THEN
	HStatCalculateAsynchronous(DatabaseConnection, FileArray, hBackgroundTask, Callback_HStatCalculateAsynchronous)
ELSE
	HStatCalculateAsynchronous(DatabaseConnection, FileArray, Callback_HStatCalculateAsynchronous)
END
	INTERNAL PROCEDURE Callback_HStatCalculateAsynchronous(MyFile is string, MyResult is boolean)
		IF MyResult THEN
			Trace("Statistics calculated for the table " + MyFile)
		ELSE
			Trace("Unable to calculate statistics for the table " + MyFile + ErrorInfo())
		END
	END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`HStatCalculateAsynchronous_Callback(<Table> , <Calculation result>)`
---

**`<Table>: (Character string)`**

Name of the HFSQL Client/Server table manipulated. 

**`<Calculation result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u> if the calculation was performed,

- <u><u><u><u>False</u></u></u></u> if a problem occurred (table locked by an application for example). [HErrorInfo](../WDLang4/3044071.md) is used to identify the error.







## Voir aussi

[HStatCalculateAsynchronous (Function)](../WDLang4/1410091108.md)

