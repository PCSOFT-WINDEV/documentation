


## HDeleteFile (Function)

***In french: HSupprimeFichier***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Deletes HFSQL tables (.fic, .ndx, .ftx, .sdx and .mmo files, if any) from the server.




<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
MyConnection is Connection
// Deletes a table from the server 
IF HDeleteFile(MyConnection, ".\save\fileclient_sov.fic") = False THEN
	Error(HErrorInfo())
END
// To delete a table in the database directory
// HDeleteFile(MyConnection, "Client.FIC")
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = HDeleteFile(<Connection> , <Table path> [, <Options>])`
---

**`<Result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u> if the table has been deleted,

- <u><u><u><u>False</u></u></u></u> if an error occurred (the table does not exist on the server for example). [HError](../WDLang4/3044088.md) is used to identify the error.




**`<Connection>: (Character string or Connection variable)`**

Connection in which the table must be deleted. This connection corresponds to:

- the name of the connection defined in the data model editor or with [HDescribeConnection](../WDLang4/3044205.md) or [HOpenConnection](../WDLang4/3044107.md).

- the name of a variable of type [Connection](../WDLang4/1514073.md).




**`<Table path>: (Character string)`**

Physical name and path of the table to delete. The path is relative to the database: the database name must not be specified in the path.. The ".fic" extension of the table must be specified.

**`<Options>: (Optional Integer constant (or combination of constants))`**

By default, when deleting a table:

- the files corresponding to the logs are not deleted, 

- the files corresponding to the transactions are deleted. 




This parameter is used to modify this behavior:



|   |   |
| --- | --- |
| *hDeleteLog* | Deletes the Log file and all the logged operations associated with the deleted table. |
| *hDestroyLink* | Deletes all links related to the deleted table. |
| *hKeepTRS* | Keeps the transaction file associated with the deleted table. |





<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Deleting a table
<a name="deleting_table_ELTPARAGRAPHE000126"></a>

The table can be deleted only if it is not currently used (which means if it is not used by a query, a view, another window, a thread or a component with independent HFSQL context).

The table is automatically closed in the HFSQL context that calls **HDeleteFile**. A WLanguage error occurs if the table is used in a context or by an application.
<a name="NOTE0_2"></a>


### Names of files and directories on the LINUX server
<a name="names_files_and_directories_the_linux_server_ELTPARAGRAPHE000139"></a>

On a LINUX server, the file names corresponding to the tables (.fic, .ftx, .sdx, .ndx and .mmo tables if they exist) must be in lower case and without accents.

<a name="XComposante"></a>

## Component:
wd310hf.dll
## Voir aussi

[fDelete (Function)](../WDLang4/3036029.md)

[HCreation (Function)](../WDLang4/3044255.md)

[HCreationIfNotFound (Function)](../WDLang4/3044254.md)

[HFSQL Client/Server functions](../WDLang4/3044316.md)

[&lt;Connection variable&gt;.DeleteFile (Function)](../WDLang4/1000022757.md)

