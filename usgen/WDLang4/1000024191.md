


## &lt;Source&gt;.Alias (Function)

***In french: &lt;Source&gt;.Alias***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Creates a logical alias of a table (or a query). The alias can be canceled using [HCancelAlias](../WDLang4/3044149.md).

**Reminder** An alias allows you to work on different physical tables with the same logical description. For a table for example, the table description is performed in the database schema and **&lt;Source&gt;.Alias** is used to create the table alias.**Note** This help page describes how to create an alias using the function **&lt;Source&gt;.Alias**. From version 27 onwards, we recommend using a [Alias](../WDLang4/1000021446.md) variable. This type of variable makes it easier to create and handle aliases. 

<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Data Source variable to handle the alias after its creation
OrderCheck is Data Source <description=Order>
// New feature of version 19: 
// <description=Order> is used to get the code completion 
// for the items in the Order table

// Create an alias for the "Order" table
IF Order.Alias(OrderCheck) = True THEN
	// "OrderCheck" can now be used in processes.
	// It behaves like the "Orders" table described in the database schema.
	// By default, the name of the associated physical file corresponds to 
	// the name of the data source, therfore "OrderCheck" in this case
	// To handle the same physical file as the initial one, the name must be modified:
    OrderCheck.ChangeName(Order.PhysicalName)
    	// To use a file on another database or in another directory,
	// then use <Source>.ChangeConnection or <Source>.ChangeDir
ELSE
    Error("Failure creating the alias", HErrorInfo())
    RETURN
END
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = <Source>.Alias([<Alias>])`
---

**`<Result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u> if the alias was created,

- <u><u><u><u>False</u></u></u></u> if a problem occurs. [HError](../WDLang4/3044088.md) is used to identify the error.




**`<Source>: (Type corresponding to the specified source)`**

Logical name of table (defined in the database schema) or name of query (created with the query editor). 

**`<Alias>: (Optional character string or data source)`**

Name of alias, used to define the alias structure. 
If this parameter is not specified, all the aliases created for &lt;Source&gt; are canceled. The corresponding tables are closed. 
To optimize your applications, it is recommended to use a variable of type [Data source](../WDLang4/1514053.md) (see example). 

![OLE DB](https://doc.pcsoft.fr/ext/images/us/OLEDB.png)![Native Connectors (Native Accesses)](https://doc.pcsoft.fr/ext/images/us/AN.png) The &lt;Alias&gt; file uses the connection parameters of &lt;Source&gt; (parameters defined in the database schema).



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Physical table associated with an alias
<a name="physical_table_associated_with_alias_ELTPARAGRAPHE000292"></a>

By default, the name specified for the alias is assigned to the physical table corresponding to the alias. 

To associate the alias with a physical table of different name, you must use [&lt;Source&gt;.ChangeConnection](../WDLang4/1000024199.md), [&lt;Source&gt;.ChangeName](../WDLang4/1000024201.md) and [&lt;Source&gt;.ChangeDir](../WDLang4/1000024202.md).
<a name="NOTE0_2"></a>


### Canceling the aliases of one or more tables
<a name="canceling_the_aliases_one_more_tables_ELTPARAGRAPHE000319"></a>

The cancellation of an alias ([HCancelAlias](../WDLang4/3044149.md)) closes the associated table.

To cancel the aliases of a table, all you have to do is use **&lt;Source&gt;.Alias** without specifying the alias name.


<a name="NOTE0_3"></a>


### Alias and queries
<a name="alias_and_queries_ELTPARAGRAPHE000342"></a>

- **If the query was created through programming** ([&lt;Source&gt;.ExecuteSQLQuery](../WDLang4/1000024948.md)), all you have to do is create the alias query with this same function while specifying the alias name.

- **If the query was created by the report editor**, to create an alias, you must:

	- run the initial query. 

	- use **&lt;Source&gt;.Alias** and specify the query name as well as the alias name. 

	- use [&lt;Source&gt;.ExecuteQuery](../WDLang4/1000024944.md) with the alias query. The alias is initialized.


 For example:
	
	```wl
	QueryAlias is Data Source
	QRY_Query1.Alias(QueryAlias)
	QueryAlias.ExecuteQuery()
	```

	**Note** When creating the alias, the initial query must have been executed.
	Example:
	
	```wl
	QueryAlias is Data Source 
	IF QRY_Query1.ExecuteQuery() = False THEN
		Error(HError(hErrCurrent)) 
	ELSE
		QRY_Query1.ReadFirst()
		Trace(QRY_Query1.City)
		QRY_Query1.Alias(QueryAlias)
		IF QueryAlias.ExecuteQuery() = False THEN
			Error(HError(hErrCurrent)) 
		ELSE
			FOR EACH AliasQuery
				Trace(QueryAlias.City)
			END
		END
	END
	```




<a name="NOTE0_4"></a>


### Handling the alias file and its items
<a name="handling_the_alias_file_and_its_items_ELTPARAGRAPHE000388"></a>

- To avoid the "Unknown identifier" error when using an alias file and its items, it is recommended to use a [Data source](../WDLang4/1514053.md) variable to declare the name of the alias before using **&lt;Source&gt;.Alias**.
	For example: 
	
	```wl
	Order2018 is Data Source
	Order.Alias(Order2018)
	```

	Warning: use of the keyword **External** keyword is possible, but causes a significant slowdown at runtime.

- In functions [&lt;Source&gt;.ReadSeek](../WDLang4/1000025023.md), [&lt;Source&gt;.ReadFirst](../WDLang4/1000025022.md), [&lt;Source&gt;.ReadNext](../WDLang4/1000025035.md), ... the alias field name cannot be used directly: the field name must be enclosed in quotation marks. 
	For example:
	
	```wl
	srcAlias.ReadSeekFirst("KeyItem", "Sought Value")
	```


- The name of the data source must not be used in quotation marks, if [Automatic naming of data sources](../Editeurs/9500218.md) is enabled in the project description window.

- In the window or page editor, controls cannot be directly bound to the items of the alias files. You must:

	- assign a value to each control in the code.

	- use the [ControlAlias](../WDLang1/3025016.md) function or the [DataBinding](../Proprietes/2510060.md) property before calling: 

		- the [&lt;Window&gt;.FromFile](../WDLang4/1000021387.md) function.

		- the [&lt;Page&gt;.FromFile](../WDLang4/1000021389.md) function.




- The alias files can be handled by [&lt;Source&gt;.ListItem](../WDLang4/1000025017.md) and [&lt;Source&gt;.ListKey](../WDLang4/1000025012.md). The alias files can be reindexed.

- By default, the **password of a table alias** is identical to the password of the initial table. This password can be changed using [&lt;Source&gt;.Password](../WDLang4/1000025044.md). The name of the table used in this function corresponds to the name of the alias.

- The automatic modification of the tables can be applied to the alias files.

- The locks performed on the tables or on the records are effective between the aliases of the same instance of an application.



<a name="NOTE0_5"></a>
![WINDEV](https://doc.pcsoft.fr/ext/images/us/WD.png)![WEBDEV - Server code](https://doc.pcsoft.fr/ext/images/us/WBS.png)![Reports and Queries](https://doc.pcsoft.fr/ext/images/us/ER.png)![iPhone/iPad](https://doc.pcsoft.fr/ext/images/us/IPHONE.png)![IOS Widget](https://doc.pcsoft.fr/ext/images/us/WIDGETIOS.png)![Mac Catalyst](https://doc.pcsoft.fr/ext/images/us/CATALYST.png)![Java](https://doc.pcsoft.fr/ext/images/us/JAVA.png)![User code (UMC)](https://doc.pcsoft.fr/ext/images/us/MCU.png)![Ajax](https://doc.pcsoft.fr/ext/images/us/AJAX.png)![Stored procedures](https://doc.pcsoft.fr/ext/images/us/PS.png) 

### Changing the directory or modifying the connection
<a name="changing_the_directory_modifying_the_connection_ELTPARAGRAPHE000492"></a>

If [&lt;Source&gt;.ChangeDir](../WDLang4/1000024202.md) or [HConnect](../WDLang4/3044263.md) have been used with the initial table, these changes are ignored for the &lt;Alias&gt; table. 

Once an alias was created, we recommend that you use [&lt;Source&gt;.ChangeConnection](../WDLang4/1000024199.md) to define the connection parameters on the alias file.

<a name="XComposante"></a>

## Component:
wd310hf.dll
## Voir aussi

[HCancelAlias (Function)](../WDLang4/3044149.md)

[&lt;Source&gt;.ChangeDir (Function)](../WDLang4/1000024202.md)

[HSubstDir (Function)](../WDLang4/3044028.md)

[&lt;Source&gt;.ChangeName (Function)](../WDLang4/1000024201.md)

[HFSQL functions (prefix syntax)](../WDLang4/1000025085.md)

[HAlias (Function)](../WDLang4/3044176.md)

