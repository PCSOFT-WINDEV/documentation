


## HComputer (Function)

***In french: HPoste***

<DIV class="specObsolete">
	<IMG style="float:left;margin-right:10px;" src="$IMGLOCAL$=warning_big.png"><B>Warning</B><BR>
	From version <B>25 (89)</B>, ***HPost*</**>** is kept for backward compatibility. This function has been replaced with ***HComputer*</**>**.
</DIV><a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Stores a unique computer number or identifier in order to use the log and transactions over the network. The computer corresponds to the user computer. 

By default, the computer is identified by a unique number and by the computer name (defined in Windows). To easily identify the computer that runs the transactions, **HComputer** sets an ID specific to the computer. This ID replaces the computer name and can be viewed in [WDTrans](../WDTrans/3524005.md) and [WDLog](../WDJournal/3516012.md).
<a name="sample_code"></a>

## Example

<a name="Exemple1"></a>

```wl
HComputer("45000")
```
<a name="Exemple2"></a>

```wl
// Assign the computer number read in "COMPUTERNUM" entry
// of "MYPROGRAM" section of "WIN.INI" file
ComputerNum is a string
ComputerNum = INIRead("MYPROGRAM", "COMPUTERNUM", "")
IF ComputerNum = "" THEN
	Error("Problem retrieving the computer number")
ELSE
	HComputer(ComputerNum)
END
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = HComputer(<Identifier>)`
---

**`<Result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u> if the computer number was stored,

- <u><u><u><u>False</u></u></u></u> otherwise. [HError](../WDLang4/3044088.md) returns more details about the problem.




**`<Identifier>: (Character string)`**

Identifies the current computer (up to 16 characters). This ID can be viewed in WDTrans and WDLog.. This ID corresponds to the name of the client computer (returned by [NetMachineName](../WDLang3/3056014.md)) or to the name of the computer where the tables are located (for a remote database schema).  



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


- In most cases, **HComputer** is used in network in association with a parameter file found on the local computer and indicating its number.

- **HComputer** is useless if the tables do not have a log and/or if they are not the source of a transaction.

- If **HComputer** is used after [HTransactionStart](../WDLang4/3044002.md), the computer number will be modified for the next operations performed.




<a name="XComposante"></a>

## Business / UI classification:
Business Logic
## Component:
wd310hf.dll
## Voir aussi

[Transactions: Secure your processing of HFSQL tables](../WDLang4/3044335.md)

[The log process ](../WDLang4/3044184.md)

[HFSQL functions](../WDLang4/3044156.md)

