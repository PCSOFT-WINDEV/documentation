


## XMLValidDocument (Function)

***In french: XMLDocumentValide***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Validates an XML document from an XSD schema.


<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Validation from the schema defined during the declaration
cMyDoc is xmlDocument, description = "ProjectXSDSchema"
IF XMLValidDocument(cMyDoc) = False THEN
	Error(ErrorInfo())
ELSE
	Info("The XML document is valid") 
END
```
<a name="Exemple2"></a>

```wl
// Validation with the schema passed as parameter
cMyDoc2 is xmlDocument
IF XMLValidDocument(cMyDoc2, "XSDSchemaOfProject") = False THEN
	Error(ErrorInfo())
ELSE
	Info("The XML document is valid") 
END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`<Result> = XMLValidDocument(<XML document> [, <XSD schema>])`
---

**`<Result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u> if the validation is successful,

- <u><u><u><u>False</u></u></u></u> otherwise. If an error occurs, use [ErrorInfo](../WDLang1/3013008.md) for more details on the error. 




**`<XML document>: (xmlDocument)`**

Name of the [xmlDocument](../WDLang5/1000018782.md) variable containing the document to validate.

**`<XSD schema>: (Optional character string)`**

Name of schema that will be used to validate the XML document. 

- If this parameter is specified, the specified XSD schema is used to validate the document. 

- If this parameter is not specified, the schema indicated at declaration time is used (declaration using the syntax: cMonDoc is an xmlDocument, description="monXSD"). 

- A WLanguage error is displayed if the schema specified during the declaration is not valid. 


Note: If the XML document has been declared with an XSD schema description, validation is performed automatically at save time using the [XMLSave](../WDLang5/1000019026.md) function.



<a name="XComposante"></a>

## Business / UI classification:
Business Logic
## Component:
wd310xml.dll
## Voir aussi

[xmlDocument (Variable type)](../WDLang5/1000018782.md)

[XMLSave (Function)](../WDLang5/1000019026.md)

[XML functions](../WDLang5/3081005.md)

[Managing the XSD](../WDLang5/1000019170.md)

[&lt;xmlDocument variable&gt;.ValidDocument (Function)](../WDLang5/1000023892.md)

