
## SysListScreenResolution (Function)

***In french: SysListeRésolutionEcran***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Returns the possible resolutions for a screen.
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Ajoute dans une table contenant 3 colonnes 
// la liste des résolutions disponibles pour l'écran principal
ListeAjoute(TABLE_Résolution, SysListeRésolutionEcran())
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = SysListScreenResolution([<Screen name>])`
---

**`<Result>: (Character string)`**

Character string containing the list of available resolutions, empty string ("") if an error occurred. [ErrorInfo](../WDLang1/3013008.md) returns more information on the error.

This string has the following format:

```txt
<Résolution Horizontale 1> + TAB + <Résolution Verticale 1> + TAB +
<Nombre de bits par pixels 1> + TAB + <Fréquence de rafraîchissement> + RC + ... + 
<Résolution Horizontale N> + TAB + <Résolution Verticale N> + TAB + 
<Nombre de bits par pixels N> + TAB + <Fréquence de rafraîchissement N>
```


**`<Screen name>: (Optional character string)`**

Name of the screen for which the available resolutions are requested. If this parameter is not specified, the available resolutions are sought for the main screen. If the name of the screen does not exist, &lt;Result&gt; corresponds to an empty string ("").

**Remark**: The list of screen names can be known using the [SysListScreen](../WDLang1/1000017014.md) function..  



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>






<a name="XComposante"></a>

## Component:
wd310std.dll
## Voir aussi

[System functions](../WDLang1/3073010.md)

[SysScreenRectangle (Function)](../WDLang1/1000019421.md)

