
## HTMLEnableDebugger (Function)

***In french: HTMLActiveDébogueur***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Enables the HTML/JavaScript debugger on CEF-based HTML controls: HTML Display or HTML Editor control. 

To debug controls:

- Open Google Chrome:. 

- Enter the following address: 
	
	```txt
	chrome://inspect/#devices
	```


- On the page that appears, check "Discover network targets" if necessary. 

- Click "Configure". <br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=214&name=HTMLActiveDebogueur.gif)


- In the popup window that appears, enter the following address: 
	
	```txt
	http://localhost:8088/
	```
 where 8088 is the port you have chosen. 

- Click "Done". 




**Note**: Until version 2025 Update 3, the following steps were required:

- Open Google Chrome and enter the following address: 
	
	```txt
	http://localhost:8088/
	```


- Click one of the links displayed in Chrome (1 link per control). 



<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Project initialization code
IF LoadParameter("Debug") = "1" THEN
      HTMLEnableDebugger(8081)
END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`HTMLEnableDebugger(<Port number>)`
---

**`<Port number>: (Integer)`**

Port number used for debugging. By default, this number is 8088. 



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


- This function must be called before creating any HTML control (HTML Display or HTML Editor control), for example, in the project initialization. 

- The HTML/JavaScript debugger (Devtools) can be displayed with the following URL: 
	
	```txt
	http://machine_name:port_num/
	```
For example: 
	
	```txt
	http://localhost:8088/
	```


- The debugger is enabled by default when a test is launched via GO, but is not enabled during the execution of the application. It can be used to trace the JavaScript code added with [ExecuteJS](../WDLang1/1000023014.md), for example. 




<a name="XComposante"></a>

## Business / UI classification:
Neutral code
## Component:
wd310obj.dll
## Voir aussi

[HTML Display control functions](../WDLang1/1000026264.md)

[HTML Editor control functions](../WDLang1/1000026295.md)

