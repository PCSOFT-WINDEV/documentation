
## WaitWindowStart (Function)

***In french: FenAttenteDébut***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Opens the loading window of application. The user cannot access the graphic application interface as long as the loading window of application is displayed.
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Downloads an image from Internet and displays it in an Image control of application

// Display the loading window
WaitWindowStart("Downloading the image...")

// Start the download thread
ThreadExecute("MyThread", threadNormal, procDownloadImage)

	INTERNAL PROCEDURE procDownloadImage
		// Download the image
		IF HTTPRequest("https://www.windev.com/img/visual/21/logo-pcsoft.png") THEN
			bufImage is Buffer = HTTPGetResult()
			// Display the downloaded image in an Image control of the window
			ExecuteMainThread(procUpdImage)
				INTERNAL PROCEDURE procUpdImage
					IMG_NoName1 = bufImage
				END
			// Close the loading window
			WaitWindowEnd()
		END
	END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`WaitWindowStart(<Message>)`
---

**`<Message>: (Character string)`**

Message to display in the loading window.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


- To close the loading window, [WaitWindowEnd](../WDLang1/1000023417.md) must be called as many times as **WaitWindowStart** was called.

- A single loading window can be displayed at a time. If **WaitWindowStart** is called while the loading window is already opened, the message displayed will be refreshed with the message specified during the last call.

- **WaitWindowStart** and [WaitWindowEnd](../WDLang1/1000023417.md) can be called from a secondary thread.

- The loading window displays an infinite progress bar. In order for this progress bar to be displayed and animated, you must give control back to the main application thread when running the process that requires a wait. This process should be performed in a secondary thread or in a parallel task. If the process must access the GUI, call [ExecuteMainThread](../WDLang1/1000019862.md) to perform these accesses.

- ![Android](https://doc.pcsoft.fr/ext/images/us/ANDROID.png) The loading window opened by **WaitWindowStart** uses the same window as the one displayed for the application hourglass ([Hourglass](../WDLang1/3071011.md)). The calls to [Hourglass](../WDLang1/3071011.md) and **WaitWindowStart**/[WaitWindowEnd](../WDLang1/1000023417.md) should not be nested.

- ![Android](https://doc.pcsoft.fr/ext/images/us/ANDROID.png) The loading window is linked to the current window when it was opened. If this window is closed, the loading window will also be closed.




<a name="XComposante"></a>

## Business / UI classification:
UI Code
## Component:
wd310android.jar
## Voir aussi

[WaitWindowEnd (Function)](../WDLang1/1000023417.md)

[Window functions](../WDLang1/3038012.md)

[HourGlass (Function)](../WDLang1/3071011.md)

