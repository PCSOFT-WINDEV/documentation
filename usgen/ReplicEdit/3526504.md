
## Add tables to a replication (ReplicEdit)
			



<a name="NOTE1"></a>
<a name="NOTE1_1"></a>


## Overview
<a name="overview_ELTTEXTE000107"></a>
When the replication is described, the tables must be added into the replication. This operation is performed via a wizard. 

<a name="NOTE2"></a>
<a name="NOTE2_1"></a>


<a name="NOTE3"></a>
<a name="NOTE3_1"></a>


## Adding tables into a replication
<a name="adding_tables_into_replication_ELTTEXTE000131"></a>


### Adding the base file
<a name="adding_the_base_file_ELTPARAGRAPHE000021"></a>

1. Select "Replication .. Add a table". The wizard for adding a table is launched.

2. The list of tables described in the current database schema or found on the server is displayed. <br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=214&name=ReplicEdit_FichierDeBase.gif&type=thumb)
You have the ability to select one or more tables to synchronize. All the records are replicated by default. 

3. For the selected tables, you can define: 

	- the replication direction for the tables:<br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=214&name=ReplicEdit_FichierSens.gif&type=thumb)


	- the management mode of modification conflicts. <br>![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=214&name=ReplicEdit_FichierConflit.gif&type=thumb)

			You can choose: 

	- the default mode defined beforehand (recommended). 

	- another mode. 

4. The wizard is finished. The replication is described. The replication editor lists the tables taken into account in the replication. 




Tables displayed in grayed are linked tables: a linked table is a table that is linked to a base table (already part of the replication). Depending on the replication, data from the linked table may or may not be added. 

You can save the replication ("File .. Save"). The replication is saved in the format of a ".Wer" file. 

This file contains the description of the replication. It must be found beside the executable on the subscriber computers.


### Add a linked table
<a name="add_linked_table_ELTPARAGRAPHE000047"></a>

To add a linked table:

1. Double click on the linked table (shown in gray) in ReplicEdit. The wizard for adding a linked table starts.

2. By default, the data in the linked table are those corresponding to the database file. Therefore, if the base file is filtered, the records found in the linked file use the same filter. 
	Note: To filter the base file, display the [table description](../ReplicEdit/3526506.md). 

3. Specify whether an additional filtering must be performed. If you add a filtering, specify the necessary characteristics. 

4. In the rest of the wizard, specify the replication direction of the linked table and the conflict management mode.

5. Finish the wizard. The linked table now appears as a table to be replicated.





## Voir aussi

[Creating a replication](../ReplicEdit/3526501.md)

[Description of replicated tables (ReplicEdit)](../ReplicEdit/3526506.md)

[ReplicEdit: Replication editor](../ReplicEdit/3526500.md)

