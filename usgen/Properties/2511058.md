


## NbPrintedRec (Property)

***In french: NbEnrImprimé***
	



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
The **NbPrintedRec** property is used to determine the number of records already printed.

**Remarque**: The **NbEnrImprimé** property ignores repeated blocks down to the bottom of the page.


<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// If more than 5 records have been printed
IF ETAT_EtatClient.NbEnrImprimé > 5 THEN
	// The print of "RPT_CustomerReport" is stopped
	iEndReport()
END
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = <Report used>.NbPrintedRec`
---

**`<Result>: (Integer)`**

Number of records already printed.

**`<Report used>: (Report name)`**

Name of the report to be used.  



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Number of records
<a name="number_records_ELTPARAGRAPHE000042"></a>

- The [NbRecRead](../Proprietes/2511041.md) property returns the number of records selected by the source of the report (query, view, memory area or Table control).

- The **NbPrintedRec** property returns the number of records selected both by the source of the report AND by the filter code (if any).
	In the case of a file report, the [NbRecRead](../Proprietes/2511041.md) and **NbPrintedRec** return the same value: the number of records in the file AND selected by the filter code (if any).

- The [TotalNbRec](../Proprietes/2511047.md) property is used to determine and change the number of records to print. The number of records to print will be between 1 and **NbPrintedRec**.



<a name="NOTE0_2"></a>


### Limit
<a name="limit_ELTPARAGRAPHE000068"></a>

The **NbPrintedRec** property cannot be used on reports without a data source.


## Voir aussi

[NbRecRead (Property)](../Proprietes/2511041.md)

[TotalNbRec (Property)](../Proprietes/2511047.md)

[Properties associated with reports and controls](../Proprietes/2515010.md)

