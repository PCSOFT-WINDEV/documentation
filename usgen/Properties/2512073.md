


## Replication (Property)

***In french: Replication***
	



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
The **Replication** property is used to:

- find out the replication mode used for a table (table defined in the data model editor or programmatically defined).

- specify the replication mode used for a table (for programmed description of the table only).




**This property can only be used in a log-based replication**.
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Checks whether the table is configured for replication (log-based replication)
IF Customer.Replication = hNotReplicated THEN Info("Unable to replicate")
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

### Getting the replication mode

`<Current mode> = <Table>.Replication`
---

**`<Current mode>: (Integer constant)`**

Replication modes:


|   |   |
| --- | --- |
| *hMasterReplica* | The table is a master replica ([HCreateMasterReplica](../WDLang4/3044175.md) was used to create the master replica) |
| *hNotReplicated* | The table is not configured for a log-based replication. To implement log-based replication, check "Log-based replication" in the table options ("Logging" tab of the table description window). |
| *hReplicated* | The table is configured to be replicated. |
| *hSubscriberReplica* | The table is a subscriber replica ([HCreateSubscriberReplica](../WDLang4/3044253.md) was used to create the subscriber replica) |



**`<Table>: (Character string)`**

Name of the table used. This name is defined in the data model editor or with the [File Description](../WDLang4/1514065.md) type.  


<a name="SYNTAXE2"></a>

### Defining the replication mode

`<Table>.Replication = <Replication mode>`
---

**`<Table>: (Character string)`**

Name of the table used. This name is defined with the [File Description](../WDLang4/1514065.md) type.

**`<Replication mode>: (Integer constant)`**

Replication modes:


|   |   |
| --- | --- |
| *hNotReplicated* | The table is not configured for a log-based replication. |
| *hReplicated* | The table is configured to be replicated. |






## Voir aussi

[HFSQL properties](../Proprietes/2512070.md)

[Log-based replication](../Proprietes/3044256.md)

