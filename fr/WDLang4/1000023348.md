
## MemcachedCAS (Fonction)

***En anglais : MemcachedCAS***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Modifie la valeur d'une clé si celle-ci n'a pas été modifiée.
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Exemple


```wl
MemcachedCAS(memConnexion, "clé", "valeur")
```

<a name="XSYNTAXE"></a>

## Syntaxe
<a name="SYNTAXE1"></a>

`<Résultat> = MemcachedCAS(<Connexion> , <Clé> , <Valeur> , <Cas> [, <Expiration> [, <Valeur supplémentaire>]])`
---

**`<Résultat> : (Booléen)`**



- <u><u><u><u>Vrai</u></u></u></u> si l'opération a été réalisée, 

- <u><u><u><u>Faux</u></u></u></u> en cas d'erreur. Pour connaître le libellé de l'erreur, utilisez la fonction [ErreurInfo](../WDLang1/3013008.md) avec la constante *errMessage*.




**`<Connexion> : (Variable de type memcachedConnexion)`**

Nom de la variable de type [memcachedConnexion](../WDLang4/1000023339.md) correspondant à la connexion au cache Memcached. 

**`<Clé> : (Chaîne de caractères)`**

Clé à modifier. 

**`<Valeur> : (Chaîne de caractères ou Buffer)`**

Valeur à stocker. 

**`<Cas> : (Entier)`**

Valeur de la version précédente. 

**`<Expiration> : (Entier ou durée optionnelle)`**

Durée pendant laquelle la clé est conservée dans le cache. Cette durée est exprimée en secondes. Cette durée peut correspondre à : 

- un entier correspondant au nombre de secondes, 

- une variable de type Durée. 

- l'indication directe de la durée (par exemple '10s'). 


Par défaut, cette durée vaut 0. Dans ce cas, la durée est illimitée : s'il n'y a plus de mémoire disponible, les clés les plus anciennes sont supprimées.

**`<Valeur supplémentaire> : (Entier optionnel)`**

Permet de stocker une valeur supplémentaire associée au couple Clé/Valeur. Ce paramètre peut permettre par exemple de décrire la clé ou la valeur.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarques
Pour utiliser cette fonction, le serveur Memcached doit supporter le CAS (propriété **SupporteCAS** de la variable de type [memcachedConnexion](../WDLang4/1000023339.md) à <u><u><u><u>Vrai</u></u></u></u>). Le CAS (Check And Set) permet de réaliser une opération de modification des données uniquement si cette donnée n'a pas été modifiée depuis sa dernière lecture.

<a name="XComposante"></a>

## Composante :
wd280big.dll
