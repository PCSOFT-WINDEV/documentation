


## &lt;Liste&gt;.SelectPlus (Fonction)

***En anglais : &lt;List Box&gt;.SelectPlus***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Sélectionne un ou plusieurs éléments dans un champ Liste, Liste image ou Combo.

L'élément est sélectionné mais n'est pas affiché s'il n'est pas visible. Pour rendre l'élément sélectionné visible, utilisez la fonction [&lt;Liste&gt;.Position](../WDLang1/1000017335.md). 

**Rappel** : L'élément sélectionné correspond à l'élément en cours sur lequel le bandeau de sélection est affiché.

**Remarque** : La fonction **&lt;Liste&gt;.SelectPlus** peut être utilisée indifféremment sur :

- les champs Liste fichier ou Liste par programmation,

- les champs Liste monosélection ou multisélection.











<a name="Exemple1"></a>
<a name="sample_code"></a>

## Exemple


```wl
// Sélection de l'élément dont l'indice est 7 dans le champ "LISTE_ListeClient"
LISTE_ListeClient.SelectPlus(7)
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntaxe

`<Champ Liste>.SelectPlus([<Indice 1> [... [, <Indice N>]]])`
---

**`<Champ Liste> : (Nom de champ)`**

Nom du champ Liste (ou Combo) à manipuler.

**`<Indice 1> : (Entiers optionnels)`**

Indice du premier élément à sélectionner. 
Si aucun indice n'est précisé, tous les éléments du champ Liste (ou Combo) spécifié sont sélectionnés. 
Si ces paramètres correspondent à des éléments déjà sélectionnés, la fonction **&lt;Liste&gt;.SelectPlus** est sans effet.



**`<Indice N> : (Entier optionnel)`**

Indice du Nième élément à sélectionner. 
Si aucun indice n'est précisé, tous les éléments du champ Liste (ou Combo) spécifié sont sélectionnés. 
Si ces paramètres correspondent à des éléments déjà sélectionnés, la fonction **&lt;Liste&gt;.SelectPlus** est sans effet.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarques


### Dernier élément
<a name="dernier_element_ELTPARAGRAPHE000262"></a>Pour se positionner sur le dernier élément d'un champ Liste, la syntaxe est :

```txt
<Champ Liste>.SelectPlus(<Champ Liste>.Occurrence)
```
ou

```txt
<Champ Liste> = <Champ Liste>.Occurrence
```

<a name="NOTE0_2"></a>


### Équivalence
<a name="equivalence_ELTPARAGRAPHE000284"></a>Dans le cas d'un champ Liste monosélection sans l'utilisation de la fonction [gValeurMémorisée](../WDLang1/1410088363.md), les deux syntaxes suivantes sont équivalentes :

```txt
<Champ Liste>.SelectPlus(<Indice>)
```


et

```txt
<Champ Liste> = <Indice>
```

<a name="NOTE0_3"></a>


### Divers
<a name="divers_ELTPARAGRAPHE000306"></a>

- Pour sélectionner un élément dans un champ Table, utilisez la fonction [&lt;Table&gt;.SelectPlus](../WDLang1/1000024287.md).

- La fonction **&lt;Liste&gt;.SelectPlus** peut être utilisée sur une colonne d'un champ Table de type "Combo".

- La fonction [&lt;Liste&gt;.SelectMoins](../WDLang1/1000017393.md) permet de désélectionner les éléments d'un champ Liste ou d'un champ Combo.



<a name="NOTE0_4"></a>

<a name="XComposante"></a>

## Composante :
wd280obj.dll
