
## SocketWaitForConnection (Function)

***In french: SocketAttendConnexion***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Run on the server, this function is used to wait for a connection request coming from the client sockets.

**Remarks**:

- If the client computer uses a WINDEV application to connect to this socket, the connection request is performed by [SocketConnect](../WDLang3/3070016.md).

- **UDP socket**: **SocketWaitForConnection** cannot be used with UDP sockets.




![WEBDEV - Server code](https://doc.pcsoft.fr/ext/images/us/WBS.png) To use the sockets on the server, check "Allow server sockets" in the "Advanced" tab of the WEBDEV administrator.



**Reminder**: A socket is a communication resource used by applications to communicate from one computer to another regardless of the type of network.


<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example

![WINDEV](https://doc.pcsoft.fr/ext/images/us/WD.png)![WEBDEV - Server code](https://doc.pcsoft.fr/ext/images/us/WBS.png)![Reports and Queries](https://doc.pcsoft.fr/ext/images/us/ER.png)![User code (UMC)](https://doc.pcsoft.fr/ext/images/us/MCU.png)![PHP](https://doc.pcsoft.fr/ext/images/us/PHP.png)![Ajax](https://doc.pcsoft.fr/ext/images/us/AJAX.png) 
```wl
// Creates a socket on the server
IF SocketCreate("Server", 8000) = False THEN
	Error("Creation error " + ErrorInfo(errMessage))
ELSE
	// Manage the connections of the client computers in a thread
	ThreadExecute("Thread1", threadNormal, WaitProcedure)
END


INTERNAL PROCEDURE WaitProcedure()
	IF SocketWaitForConnection("Server") THEN
		Channel is string
		// If the connection is accepted, 
		// this connection is managed in a specific thread
		Channel = SocketAccept("Server")
		ThreadExecute(Channel, threadNormal, ProcedureManagement, Channel)
	ELSE
		IF NOT SocketExist("Server") THEN 
      		  	BREAK
		END
	END
END
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`<Result> = SocketWaitForConnection(<Socket name> [, <Maximum timeout>])`
---

**`<Result>: (Boolean)`**



- <u><u><u><u>True</u></u></u></u>: A new connection request is pending.

- <u><u><u><u>False</u></u></u></u>: No connection request has been detected.




**`<Socket name>: (Character string)`**

Name of the server socket that waits for the connection.

![WINDEV](https://doc.pcsoft.fr/ext/images/us/WD.png) Note: The socket name is case sensitive.

**`<Maximum timeout>: (Optional integer or optional Duration)`**

Maximum timeout (in milliseconds). 

- This function is a blocking function during the specified duration or until a connection request is detected.

- If this parameter is not specified, the function locks the application until the next attempt to connect. 


**Note**: If this function is executed in a multi-threaded environment, it only blocks the thread in which it is called.  

This parameter can correspond to: 

- an integer representing the number of milliseconds,

- a Duration variable,

- the duration in a readable format (e.g., 1 s or 10 ms). 









## Remarks
![Android](https://doc.pcsoft.fr/ext/images/us/ANDROID.png)![Android Widget ](https://doc.pcsoft.fr/ext/images/us/ANDROIDWIDGET.png) 

### Required permissions
<a name="required_permissions_ELTPARAGRAPHE000226"></a>

This function changes the permissions required by the application. 
Required permission: INTERNET 
This permission allows the applications to open the network sockets.

<a name="XComposante"></a>

## Component:
wd310com.dll
## Voir aussi

[SocketCreate (Function)](../WDLang3/3070011.md)

[SocketDeny (Function)](../WDLang3/3070010.md)

[Socket functions](../WDLang3/3070007.md)

[Managing sockets with WINDEV, WEBDEV and WINDEV Mobile](../WDLang3/3070008.md)

