


## &lt;Scheduler&gt;.InfoXY (Function)

***In french: &lt;Planning&gt;.InfoXY***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Returns the appointment, the resource or the date found at given position in a Scheduler control.

**Please note** The [MouseXPos](../WDLang1/3071007.md) and [MouseYPos](../WDLang1/3071008.md) functions return relevant information only in mouse- or stylus-related processing (click code, hover code, left or right button pressed code, etc.). 


<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Displays in the trace the date found at hovered location 
// (optional WM_MOUSEMOVE event)
// The result would also be obtained in the code of left button up (WM_LBUTTONUP)
// Not to be used in the code for "Selecting a period" in the Scheduler control 
// that is not linked to the mouse

MyDate is string
MyDate = SCH_Schedule1.InfoXY(schDateTime + schScreenOrigin, MouseXPos(), MouseYPos())
Trace(MyDate)
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`<Result> = <Scheduler control>.InfoXY(<Type of information> , <X> , <Y>)`
---

**`<Result>: (Character string or Integer)`**

Requested information: 

- Index of appointment at specified point, -1 if there is no appointment. 

- Date and time (in YYYYMMDDHHSSCC format) of appointment at specified point, empty string ("") if there is no date. 

- Name of resource at specified point, empty string ("") if there is no resource.




**`<Scheduler control>: (Control name)`**

Name of the Scheduler control to be used.

**`<Type of information>: (Integer constant (or combination of constants))`**

Type of information requested:


|   |   |
| --- | --- |
| *schAppointment* | Index of the appointment at the specified coordinates. |
| *schDateTime* | Date and time of the appointment corresponding to the specified coordinates. |
| *schResource* | Name of resource corresponding to specified coordinates. |
| *schScreenOrigin* | By default, the point (0,0) corresponds to the top left of the Scheduler control being manipulated (with the scrollbars at the origin).<br>If the *schScreenOrigin* constant is combined with the other constants, the (0,0) point corresponds to the origin of the screen.<br>This constant cannot be used on its own.<br><br>![iPhone/iPad](https://doc.pcsoft.fr/ext/images/us/IPHONE.png) This constant is not available. |



**`<X>: (Integer)`**

X-coordinate (in pixels) to be analyzed. This coordinate is given in relation to the Scheduler control (if the *schScreenOrigin* constant is not specified).

**`<Y>: (Integer)`**

Y-coordinate (in pixels) to be analyzed. This coordinate is given in relation to the Scheduler control (if the *schScreenOrigin* constant is not specified).



<a name="XComposante"></a>

## Component:
wd310obj.dll
## Voir aussi

[The Scheduler control](../WDLang1/1000019498.md)

[Functions for managing Scheduler controls (prefix syntax)](../WDLang1/1000022917.md)

[SchedulerInfoXY (Function)](../WDLang1/1000019553.md)

