
## WinOptimize (Function)

***In french: FenOptimise***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Changes the mode used to create and draw windows. All windows opened after calling this function will use the new mode. 

This function makes it easier to open and draw windows containing a large number of controls. 
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
WinOptimize(woWithoutHandle)
Open(WIN_Home)
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`<Result> = WinOptimize(<Mode>)`
---

**`<Result>: (Integer constant)`**

Old mode used by the windows: 


|   |   |
| --- | --- |
| *woDirect2D* | Windows use Direct 2D at runtime.<br> |
| *woWithHandle* | Windows use handles at runtime. |
| *woWithoutHandle* | Windows do not use handles at runtime. |



**`<Mode>: (Integer)`**

New mode used to create and draw windows: 


|   |   |
| --- | --- |
| *woDirect2D* | **Optimized mode**: Windows use Direct 2D at runtime. In this case, the graphics card draws the window and its controls. <br> |
| *woWithHandle* | **Default mode**: Windows use handles at runtime. In this mode, the processor is responsible for displaying and drawing windows and their controls. There can be no more than 1000 handles simultaneously.  |
| *woWithoutHandle* | **Optimized mode**: In this mode, the processor is responsible for displaying the windows and their controls. A single handle is created for the window and its controls. This constant eliminates the limitation related to the number of handles. <br>**Caution**: This mode is not compatible with certain WLanguage functions, such as [Handle](../WDLang1/3015005.md). |





<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Activation in the editor
<a name="activation_the_editor_ELTPARAGRAPHE000074"></a>

You can also activate these optimization options in the window editor. Simply open the "Modifier" pane, expand the "Details" group, select "Optimizations" and choose the desired option. 





<a name="XComposante"></a>

## Business / UI classification:
UI Code
## Voir aussi

[Window functions](../WDLang1/3038012.md)

[GDI+ framework](../WDLang1/2020007.md)

