


## PVTAxisXY (Function)

***In french: TCDAxeXY***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Allows you to: 

- Set the row and column headers displayed in a Pivot Table control. 

- Get the row and column headers displayed in a Pivot Table control. 





<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Specifies the row and column headers
IF CBOX_ReverseAxis = True THEN
	PVTAxisXY(PVT_Stats, "COL_Date" + CR + "COL_Product", "COL_Salesman")
ELSE
	PVTAxisXY(PVT_Stats, "COL_Salesman", "COL_Date" + CR + "COL_Product")
END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

### Setting the row and column headers

`PVTAxisXY(<Pivot Table control> , <List of column headers> , <List of row headers>)`
---

**`<Pivot Table control>: (Control name)`**

Name of the Pivot Table control to be used. 

**`<List of column headers>: (Character string)`**

List of column headers (displayed on the X-axis). This list contains the different column headers separated by carriage returns. 
This list can use one of the following syntaxes: 

- 
	```txt
	<Name header 1>..Name + CR + ... + CR + <Name header N>..Name
	```
Example: 
	
	```txt
	COL_Date..Name + CR + COL_Product..Name
	```


- 
	```txt
	"<Name header 1>" + CR + ... + CR + "<Name header N>"
	```
Example: 
	
	```txt
	"COL_Date" + CR + "COL_Product"
	```





**`<List of row headers>: (Character string)`**

List of row headers (displayed on the Y-axis). This list contains the different row headers separated by carriage returns. This list can use one of the following syntaxes: 

- 
	```txt
	<Name header 1>..Name + CR + ... + CR + <Name header N>..Name
	```
Example: 
	```txt
	COL_Date..Name + CR + COL_Product..Name
	```


- 
	```txt
	"<Name header 1>" + CR + ... + CR + "<Name header N>"
	```
Example: 
	```txt
	"COL_Date" + CR + "COL_Product"
	```






<a name="SYNTAXE2"></a>

### Setting the row and column headers

`<Result> = PVTAxisXY(<Pivot Table control>)`
---

**`<Result>: (Character string)`**

List of row and column headers displayed in the Pivot Table control. This list has the following format: 


```txt
<Name header column 1> + CR + ...+ CR + <Name header column N> + EOT + 
<Name header row 1> + CR + ...+ CR + <Name header row N> + EOT + 
<Name filter 1> + CR + ... CR + <Name filter N>
```
For example: 


```txt
COL_OrderDate + CR + COL__OrderDate_Quarter + CR+ 
COL_OrderDate_Month + EOT + COL_Country + CR + COL_City + EOT + PVT_Caption
```


**`<Pivot Table control>: (Control name)`**

Name of the Pivot Table control to be used.



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks


### Setting the headers
<a name="setting_the_headers_ELTPARAGRAPHE000103"></a>

- The Pivot Table control does not need to be recalculated, and its data is displayed immediately.

- The headers you specify must match the existing headers. You can for example: 

	- reverse the row and column headers. 

	- specify fewer headers than the ones defined in the control. 







<a name="XComposante"></a>

## Business / UI classification:
UI Code
## Component:
wd310obj.dll
## Voir aussi

[The Pivot Table control](../WDLang1/1000029001.md)

[Pivot Table control functions](../WDLang1/1000020704.md)

[PVTCalculateAll (Function)](../WDLang1/1000020539.md)

[&lt;Pivot Table&gt;.AxisXY (Function)](../WDLang1/1000022178.md)

