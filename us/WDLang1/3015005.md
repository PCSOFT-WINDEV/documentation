
## Handle (Function)

***In french: Handle***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Returns the system "Handle" (HWND) of a WINDEV control or window. This allows you to call some API functions with this parameters (with [API](../WDLang1/3014005.md) or [CallDLL32](../WDLang1/3014003.md), [SendMessage](../WDLang1/3015003.md) or [PostMessage](../WDLang1/3015001.md)).

This "Handle" is rarely used with WLanguage functions.

![iPhone/iPad](https://doc.pcsoft.fr/ext/images/us/IPHONE.png) Returns the UIView associated with the window. This identifier can be used when programming in Objective-C . 
 note: the UIView is not initialized in the window globals declaration code, but in the end of window initialization code.. For more details, see [Using Objective-C code](../Editeurs/1000019887.md). 

![Android](https://doc.pcsoft.fr/ext/images/us/ANDROID.png) The concept of **handle** does not exist in the Android applications. A native Java code can get a "context" if necessary with **getCurrentActivity** and **getApplicationContext**. For more details, see [Specific features of applications for Android - Using a native Java code in an Android application](../Editeurs/9000108.md). 
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Copy the content of an edit control to the clipboard
SendMessage(Handle(EDT_EDIT1), WM_COPY, 0, 0)
```


<a name="Exemple2"></a>


![WINDEV](https://doc.pcsoft.fr/ext/images/us/WD.png)![Reports and Queries](https://doc.pcsoft.fr/ext/images/us/ER.png)![User code (UMC)](https://doc.pcsoft.fr/ext/images/us/MCU.png) 
```wl
// Immediately refresh the drawing of CONTROL1
hwnd is int = Handle(CONTROL1)
CallDLL32("USER32", "UpdateWindow", hwnd)
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

### Retrieving the handle of a control or window

`<Result> = Handle([<Object name>])`
---

**`<Result>: (Pointer)`**

Requested handle.

**`<Object name>: (Optional character string)`**

Control or window name. If this parameter is not specified, **Handle** uses the current window. If this parameter corresponds to a table column, the handle of its edit control will be returned (NULL if the column is not in edit).  


<a name="SYNTAXE2"></a>

### Retrieving the handle of a specific element in a control

`<Result> = Handle([<Object name>, ] <Object type>)`
---

**`<Result>: (Integer)`**

Requested handle.

**`<Object name>: (Optional character string)`**

Control name.

**`<Object type>: (Integer constant)`**

Element of the Combo Box whose handle you want to get.


|   |   |
| --- | --- |
| *HandleAutocompleteList* | Returns the handle of the drop-down list associated with a control that has the autocomplete feature. |
| *HandleComboBoxEdit* | Returns the handle of the edit control associated with a Combo Box (the control must be a Combo Box control). |
| *HandleComboBoxList* | Returns the handle of the drop-down list associated with a Combo Box (the control must be a Combo Box control). |
| *HandleHScrollbar* | Returns the handle of the horizontal scrollbar of the control. |
| *HandleVScrollbar* | Returns the handle of the vertical scrollbar of the control. |




<a name="SYNTAXE3"></a>
![iPhone/iPad](https://doc.pcsoft.fr/ext/images/us/IPHONE.png) 
### Retrieving the handle of an iPhone/iPad window

`<Result> = Handle([<Object name>, ] <Object type>)`
---

**`<Result>: (Integer)`**

UIView associated with the window.

**`<Object name>: (Optional character string)`**

Name of the window. If this name corresponds to a control name, &lt;Result&gt; will correspond to NULL.

**`<Object type>: (Integer constant)`**

Element whose handle is sought.


|   |   |
| --- | --- |
| *HandleNativeControl* | Returns the native iOS control associated with the specified control. If this constant is used, &lt;Résultat&gt; will correspond to a non-zero value only for the following fields: Map control (MKMapView) and HTML field (WKWebView). |
| *HandleUIViewController* | Returns the UIViewController associated with the specified window. If this constant is used, &lt;Result&gt; will correspond to a non-null value for the windows only (but not for the controls). |










- ![](https://doc.pcsoft.fr/en-US/images/image.awp?langid=3&name=Thealarms.gif) ***Unit examples (WINDEV)*** : **The alarms** <br>Implementing an alarm to display an alert message in the title bar of the active window (regardless of the application). <br>The following topics are presented:<br>1/ The system functions (retrieving the handle of a window)<br>2/ Triggering a process according to a given frequency (timers)





<a name="XComposante"></a>

## Business / UI classification:
UI Code
## Component:
wd310obj.dll
## Voir aussi

[CallDLL32 (Function)](../WDLang1/3014003.md)

[SendMessage (Function)](../WDLang1/3015003.md)

[PostMessage (Function)](../WDLang1/3015001.md)

[HandleParentNext (Function)](../WDLang1/1000017312.md)

[Windows event functions](../WDLang1/3015002.md)

