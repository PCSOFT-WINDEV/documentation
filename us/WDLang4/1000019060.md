


## HClusterNodeInfo (Function)

***In french: HClusterInfoNoeud***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Returns the status of each cluster node by interrogating the coordinator.
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
sList is string 
sName is string
nStatus is int
sList = HClusterNodeInfo("clustermanager.mydomain.com", "secret")
FOR EACH STRING sNode OF sList SEPARATED BY CR
	sName = ExtractString(sNode, 1)
	nStatus = ExtractString(sNode, 2)
	SWITCH nStatus 
 	CASE 0
			Trace("The status of the " + sName + " node is unknown.")
		CASE 1
			Trace("The " + sName + " node is stopped.")
		CASE 2
			Trace("The " + sName + " node is ready to be started.")
		CASE 3
			Trace("The " + sName + " node is starting.")
		CASE 4
			Trace("The " + sName + " node is waiting for a synchronization source.")
		CASE 5
			Trace("The " + sName + " node is synchronizing.")
		CASE 6
			Trace("The " + sName + " node is started.")
		CASE 7
	 	Trace("The " + sName + " node is stopping.")
		CASE 8
			Trace("The " + sName + " node could not be determined.")
		OTHER CASE
			Trace("The " + sName + " node has an unknown status (" + nStatus + ")")
	END
END
```

<a name="XSYNTAXE"></a>

## Syntax
<a name="SYNTAXE1"></a>

`<Result> = HClusterNodeInfo(<Coordinator> , <Password>)`
---

**`<Result>: (Character string)`**

Status of the cluster nodes. The result string has the following format: 

```txt
<Name of node 1> + TAB + <Status of node 1> + CR +
<Name of node 2> + TAB + <Status of node 2> + CR +
...
<Name of node N> + TAB + <Status of node N>
```


In this string, &lt;Status of Node&gt; corresponds to one of the following values: 

| Value | Node status |
| --- | --- |
| 0 | The status of the node is unknown. |
| 1 | The node is stopped. |
| 2 | The node is ready to be started. |
| 3 | The node is being started. |
| 4 | The node is waiting for a source of synchronization. |
| 5 | The node is currently synchronized. |
| 6 | The node is started. |
| 7 | The node is being stopped. |
| 8 | The status of the node cannot be defined. |



**`<Coordinator>: (Character string)`**

DNS name of the cluster coordinator's machine.

**`<Password>: (Character string or Secret string)`**

Password used to connect to the coordinator. This password was given when [installing the cluster](../WDLang4/1000019031.md).
**Secret strings**: If you use the secret string vault, the type of secret string used for this parameter must be "ANSI or Unicode string".
To learn more about secret strings and how to use the vault, see [Secret string vault](../Editeurs/1410087353.md). 



<a name="XComposante"></a>

## Business / UI classification:
Business Logic
## Component:
wd310hf.dll
## Voir aussi

[Overview of HFSQL Cluster](../WDLang4/1000019030.md)

[Managing HFSQL Cluster](../WDLang4/1000019032.md)

[Installing and configuring HFSQL Cluster](../WDLang4/1000019031.md)

[HFSQL Cluster functions](../WDLang4/1000019033.md)

[HClusterAddNode (Function)](../WDLang4/1000019061.md)

[HClusterState (Function)](../WDLang4/1000019059.md)

[HClusterDeleteNode (Function)](../WDLang4/1000019062.md)

[HClusterMasterNode (Function)](../WDLang4/1410087885.md)

