


## HFlush (Function)

***In french: HForceEcriture***



<a name="XUtilisation"></a>
<a name="Utilisation"></a>
<a name="description"></a>
Forces the operating system of the computer where the data files are found to write data onto the disk. The HFSQL context of the manipulated data file is preserved (blocking, current path, etc.).

**Note** If used regularly, this function minimizes data loss caused by unexpected shutdowns of the workstation on which the data files are stored (e.g. power cuts).

![Java](https://doc.pcsoft.fr/ext/images/us/JAVA.png) This function is available for HFSQL data files only. It is not available for the data files accessed by JDBC. 
<a name="Exemple1"></a>
<a name="sample_code"></a>

## Example


```wl
// Add a record in a data file
// (record described through programming)
Customer.LastName = "Clark"
Customer.FirstName = "Vince"
Customer.Address = "Palm lane"
Customer.City = "San Francisco"
Customer.Zip = "94102"
Customer.Country = "USA"
HAdd(Customer)
// Forces a write operation into Customer file
HFlush(Customer)
```

<a name="XSYNTAXE"></a>
<a name="SYNTAXE1"></a>

## Syntax

`HFlush(<Data file>)`
---

**`<Data file>: (Character string)`**

Name of the data file used. 
If this name is not specified, **HFlush** will use the last data file used by the last HFSQL function (function starting with "H").
If this parameter corresponds to "\*", **HFlush** will be applied to all open data files.
**Warning** In this case, execution of the function may take a long time.
This parameter can also correspond to the name of a [group of files (or custom-folder) defined in the analysis](../Editeurs/2011003.md) (not available in Android and Android widget).



<a name="NOTE0"></a>
<a name="NOTE0_1"></a>

## Remarks
![WINDEV](https://doc.pcsoft.fr/ext/images/us/WD.png)![WEBDEV - Server code](https://doc.pcsoft.fr/ext/images/us/WBS.png)![Reports and Queries](https://doc.pcsoft.fr/ext/images/us/ER.png)![iPhone/iPad](https://doc.pcsoft.fr/ext/images/us/IPHONE.png)![User code (UMC)](https://doc.pcsoft.fr/ext/images/us/MCU.png)![Ajax](https://doc.pcsoft.fr/ext/images/us/AJAX.png)![HFSQL Classic](https://doc.pcsoft.fr/ext/images/us/HF.png)![HFSQL Client/Server](https://doc.pcsoft.fr/ext/images/us/HFCS.png) 

### Managing locks
<a name="managing_locks_ELTPARAGRAPHE000091"></a>

During the call to **HFlush**, the data file used is closed and re-opened. If locks were implemented on this data file before it was closed, these locks are automatically restored when it is re-opened. Indeed, when closing a data file, the operating system deletes the locks implemented on this data file.

**Remark**: We therefore advise against calling this function too often on:

- data files on which an important number of locks is implemented.

- data files on which an important number of write operations is implemented.



<a name="NOTE0_2"></a>


### HSecurity and HFlush
<a name="hsecurity_and_docparampagetitleshort_ELTPARAGRAPHE000109"></a>[HSecurity](../WDLang4/3044135.md) and **HFlush** can both be used to force a physical write operation on disk.

However:

- [HSecurity](../WDLang4/3044135.md) is used to force a write operation into the data files used during each write operation. The HFSQL engine automatically performs the write operation. The data files are not closed during this write operation. This results in slower write speeds.

- **HFlush** forces the data file(s) to be written when it is called. The data files are closed and then re-opened during this write operation. The write operation is not slowed down.




<a name="XComposante"></a>

## Component:
wd310hf.dll
## Voir aussi

[HFSQL functions](../WDLang4/3044156.md)

[HSecurity (Function)](../WDLang4/3044135.md)

[&lt;Source&gt;.Flush (Function)](../WDLang4/1000024981.md)

